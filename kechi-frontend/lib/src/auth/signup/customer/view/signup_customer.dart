import 'package:flutter/material.dart';

class SignupCustomer extends StatelessWidget {
  const SignupCustomer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Customer Signup Page"),
      ),
    );
  }
}
