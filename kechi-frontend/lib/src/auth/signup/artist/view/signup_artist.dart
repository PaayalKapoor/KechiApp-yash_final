import 'package:flutter/material.dart';

class SignupArtist extends StatelessWidget {
  const SignupArtist({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Artist Signup Page"),
      ),
    );
  }
}
